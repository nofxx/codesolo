.block
  .content
    - if @user
      .inner
        - unless @user.projects.empty?

        - else
          No projects.
    - elsif @tag
      %h2 ##{@tag.name}
      - for project in @tag.projects
        %p= link_to project.name
    - else
      #{params[:login]} is avaiable, wanna register it ?

- content_for :sidebar do
  .block
    - if @user
      %h2= @user.name
      = link_to @user.name
      .gray
        = @user.motto
      %ul
        %li #{@user.projects.count} projects.
    - if current_user == @user
      %ul
        %li
          = image_tag "icons/pin.png"
          = link_to 'New project!', new_project_path
    - elsif @user
      %ul
        %li
          = image_tag "icons/pin.png"
          = link_to 'Follow user!', follow_user_path(@user)


